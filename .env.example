# Ejemplo de variables de entorno para desarrollo
# NO incluir este archivo en producción ni subir secretos reales.

# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Base de datos MySQL
DB_ENGINE=django.db.backends.mysql
DB_NAME=clinica_db
DB_USER=clinica_user
DB_PASSWORD=ClinicaPass123
DB_HOST=db
DB_PORT=3306

# Media/static
MEDIA_ROOT=/media
STATIC_ROOT=/staticfiles
